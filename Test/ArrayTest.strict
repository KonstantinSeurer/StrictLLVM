
using Strict.Core.Array;

public singleton ArrayTest
{

    public ArrayTest()
    {
        Array<UInt8> bytes;
        
        for (UInt8 i = 0; i < 256; i++)
        {
            bytes.Add(i);
        }
    }
}
